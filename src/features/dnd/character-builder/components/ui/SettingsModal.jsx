// src/features/dnd/character-builder/components/ui/SettingsModal.jsx
// Tool-level settings removed — all settings live in the toolbox now.
// This stub exists so any stray imports don't break.
export default function SettingsModal() {
  return null
}