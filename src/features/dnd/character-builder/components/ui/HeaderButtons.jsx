// src/features/dnd/character-builder/components/ui/HeaderButtons.jsx
// Toolbox owns settings + bug report now. DnD renders nothing here to avoid
// duplicate UI. File kept so existing imports in pages continue to resolve.
export default function HeaderButtons() {
  return null
}